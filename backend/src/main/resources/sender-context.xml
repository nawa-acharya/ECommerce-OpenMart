<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
                           http://www.springframework.org/schema/rabbit
                           http://www.springframework.org/schema/rabbit/spring-rabbit-1.0.xsd">

        <!--  create a rabbit connection factory with specified parameters -->
    <!-- <rabbit:connection-factory id="connectionFactory"
                                 host="localhost"
                                 virtual-host="/"
                                 username="guest"
                                 password="guest"/> -->
    <rabbit:connection-factory id="connectionFactory"
                               host="wildboar.rmq.cloudamqp.com"
                               virtual-host="galqgqzm"
                               username="galqgqzm"
                               password="9QJ1IS5p1qv8tdH-xRubEI77BYRNSXn3"/>

        <rabbit:admin connection-factory="connectionFactory" />
        <!-- create a bean that sends messages to myExhange-->
        <rabbit:template id="cloudamqpTemplate" connection-factory="connectionFactory" />

        <rabbit:queue name="mySpringQueue" />



 </beans>